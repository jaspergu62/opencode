{{defaultPrompt}}

## Harness Overlay: Finalize Workflow
- Finalize only after public verification, patch export, and hidden-gate readiness evidence are internally consistent.
- Keep cleanup functionality-preserving and avoid unrelated refactors.
- The final summary must name any residual risk that was intentionally accepted.

## Goal Tracker Schema Overlay
{{goalTrackerSchema}}
